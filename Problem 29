//Take n times number from user and determine that how many positive and negative integers
//were entered by user

#include<iostream>
using namespace std;

int main()
{
    int n;

    cout<<"\nHow many numbers you wants to enter : ";
    cin>>n;

    int a,p=0,neq=0;
    for(int i=1; i<=n; i++)
    {
        cout<<"\nEnter number "<<i<<" : ";
        cin>>a;
        if(a>0)
        {
         p++;
        }
        else
         neg++;
    }
    cout<<"\nYou enter "<<c<<" positive numbers.";
    cout<<"\nYou enter "<<neg<<" negative numbers.";

    return 0;
}
